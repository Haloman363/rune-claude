{
  "permissions": {
    "defaultMode": "auto"
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.css) npx stylelint --fix \"$f\" 2>/dev/null || true ;; esac; } 2>/dev/null || true"
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) cd /home/jaymes/github-repos/rune-claude && .venv/bin/python -m pytest tests/ -q --tb=short 2>&1 | tail -20 ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.env|.env.*|*secret*|*credential*) echo \"Blocked: editing secrets file $f\" >&2; exit 2 ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
